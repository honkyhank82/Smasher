{
  "name": "smasher-api",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "cd server && npm install && npm run build",
    "start": "node server/dist/main.js",
    "android": "expo run:android",
    "ios": "expo run:ios"
  },
  "dependencies": {
    "expo": "^54.0.30",
    "react": "19.1.0",
    "react-native": "0.81.5"
  }
}
